{
  "name": "distill",
  "version": "0.1.0",
  "description": "Context distillation for Claude Code CLI — indexes session activity into FTS5 and injects relevant history via hooks",
  "type": "module",
  "bin": {
    "distill": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "better-sqlite3": "^11.7.0",
    "openai": "^6.22.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
